# Claude Code Instructions

Always read @AGENTS.md before starting any task in this repository. It contains the development environment setup (Nix dev shells), project-to-shell mapping, and all build, test, and lint commands. Every command you run must follow the conventions described there (e.g., running Go and Node tooling through the appropriate `nix develop` shell).
