export const SnapshotEvents = {
  CREATED: 'snapshot.created',
  ACTIVATED: 'snapshot.activated',
  INITIAL_RUNNER_READY: 'snapshot.initial-runner-ready',
  STATE_UPDATED: 'snapshot.state.updated',
  REMOVED: 'snapshot.removed',
} as const
