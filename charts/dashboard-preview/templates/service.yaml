apiVersion: v1
kind: Service
metadata:
  name: {{ include "dashboard-preview.fullname" . }}
  labels:
    {{- include "dashboard-preview.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dashboard-preview.selectorLabels" . | nindent 4 }}
