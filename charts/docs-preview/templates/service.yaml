apiVersion: v1
kind: Service
metadata:
  name: {{ include "docs-preview.fullname" . }}
  labels:
    {{- include "docs-preview.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docs-preview.selectorLabels" . | nindent 4 }}
