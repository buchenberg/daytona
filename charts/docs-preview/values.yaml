replicaCount: 1

image:
  repository: ghcr.io/daytonaio/daytona-docs
  tag: "latest"
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: daytonabot-token

containerPort: 4321

env:
  FUNCTIONS_PORT: "4321"

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  host: ""
  annotations: {}
  tls:
    enabled: true
    secretName: docs-stage-tls

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
